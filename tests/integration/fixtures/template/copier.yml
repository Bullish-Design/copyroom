_answers_file: .copier-answers.yml
# Preserve symlinks through copy AND update, and never render the skills the
# template ships (they contain literal {{ }} examples).
_preserve_symlinks: true
_copy_without_render:
  - ".agents/skills/**"

project_name:
  type: str
  default: myproject
