# AGENTS.md — fixture project

Generated by the CopyRoom integration test template (v1).

This file is the canonical instructions file; `CLAUDE.md` is a symlink to it.
The skill under `.agents/skills/copyroom/SKILL.md` must survive generation
byte-for-byte — including its literal `{{ project_name }}` example.
